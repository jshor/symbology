{
  "name": "zint",
  "version": "0.0.1",
  "description": "Generate 50+ different types of 1D or 2D barcodes and stream using base64 png or svg.",
  "main": "./lib/build/Release/zint.node",
  "scripts": {
    "test": "node ./node_modules/mocha/bin/mocha test/test.js"
  },
  "gypfile": true,
  "devDependencies": {
    "chai": "^3.4.1",
    "mocha": "^2.3.4"
  },
  "dependencies": {
    "segfault-handler": "^1.0.0"
  }
}
